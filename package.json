{
  "name": "echo-transcription",
  "version": "1.0.0",
  "description": "Transcription API using WhisperX and Trigger.dev",
  "main": "index.js",
  "scripts": {
    "dev": "npx trigger.dev@latest dev",
    "build": "npx trigger.dev@latest build",
    "deploy": "npx trigger.dev@latest deploy"
  },
  "dependencies": {
    "@trigger.dev/sdk": "4.0.2",
    "modal": "0.3.20"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "trigger.dev": "4.0.2",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}